/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { Operator } from "./Operator";
import { OperatorCountArgs } from "./OperatorCountArgs";
import { OperatorFindManyArgs } from "./OperatorFindManyArgs";
import { OperatorFindUniqueArgs } from "./OperatorFindUniqueArgs";
import { DeleteOperatorArgs } from "./DeleteOperatorArgs";
import { OperatorService } from "../operator.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Operator)
export class OperatorResolverBase {
  constructor(
    protected readonly service: OperatorService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Operator",
    action: "read",
    possession: "any",
  })
  async _operatorsMeta(
    @graphql.Args() args: OperatorCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Operator])
  @nestAccessControl.UseRoles({
    resource: "Operator",
    action: "read",
    possession: "any",
  })
  async operators(
    @graphql.Args() args: OperatorFindManyArgs
  ): Promise<Operator[]> {
    return this.service.operators(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Operator, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Operator",
    action: "read",
    possession: "own",
  })
  async operator(
    @graphql.Args() args: OperatorFindUniqueArgs
  ): Promise<Operator | null> {
    const result = await this.service.operator(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Operator)
  @nestAccessControl.UseRoles({
    resource: "Operator",
    action: "delete",
    possession: "any",
  })
  async deleteOperator(
    @graphql.Args() args: DeleteOperatorArgs
  ): Promise<Operator | null> {
    try {
      return await this.service.deleteOperator(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
